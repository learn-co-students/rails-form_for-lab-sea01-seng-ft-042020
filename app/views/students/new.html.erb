<h1>New Student Form:</h1>
<%= form_for(:student, url: students_path) do |t|%>
    <%= t.label(:first_name)%>
    <%= t.text_field(:first_name)%>
    <%= t.label(:last_name)%>
    <%= t.text_field(:last_name)%>
    <%= t.submit("Create Student")%>
<% end %>